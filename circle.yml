machine:  
  services:  
    - docker  
  
dependencies:  
  override:  
    - pip install -r web/requirements.txt  
  
test:  
  override:  
    - docker-compose run -d --no-deps web  
    - python web/tests.py 
